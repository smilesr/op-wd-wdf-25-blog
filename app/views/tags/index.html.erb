<h1> Available Tags </h1>

<% @tags.each do |tag| %>
<%= link_to tag.name, tag_path(tag)  %><%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "Do you really want to delete this tag?"} %> <br />


<% end %>

<%= link_to "Back to Articles", articles_path %>